<h1>STUDENTS INDEX PAGE</h1>

<section class="students">
  <% @students.sort_students.each do |student| %>
    <section id="student-<%= student.id %>" class="individual-student">
      <%= student.name %>'s Number of Professors: <%= student.count %></br></br>

      <%= student.name %>'s courses:</br>
        <% student.sort_specialty.each do |professor| %>
          <%= professor.specialty %></br>
        <% end %></br>
    </section>
  <% end %></br></br>
</section>  